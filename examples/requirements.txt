# these dependencies are not installed when Pulse SDK is installed
# These are only needed for local testing and development, hence a separate requirements file

python-dotenv==1.1.0
openai==1.88.0
langchain_openai==0.3.24
langgraph==0.4.8
langchain==0.3.25

